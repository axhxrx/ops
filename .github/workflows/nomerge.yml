name: Test NoMerge

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Reference the action from the nomerge repo
      # - uses: axhxrx/nomerge@main  # or use a specific branch
      # axhxrx/nomerge@claude/cli-stress-tests-011CUp82jyNSWX27T6LMtfzo
      - uses: axhxrx/nomerge@claude/cli-stress-tests-011CUp82jyNSWX27T6LMtfzo
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
